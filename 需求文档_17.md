# 需求文档 — 17 号项目：项目跟踪进度同步至 Kaizen & CI

> 版本：V20260606.2 | 状态：已确认

---

## 1. 项目背景

项目跟踪表（项目总表）的 E 列存储了每个项目的 Milestone JSON 数据（序号、任务描述、计划完成时间、责任人），需要将这些进度信息同步到 Kaizen & CI 总表的对应项目行中，方便统一查看。

---

## 2. 核心功能

### 2.1 数据读取

| 数据源 | 表 ID | Sheet | 字段 |
|---|---|---|---|
| 项目跟踪 | `1aoQDjeWU9Xa9clloyTwiXL6WS62tYVbB0-VOavpAgAM` | `项目总表` | A列=项目编号，E列=Milestones_JSON，D列=状态 |
| Kaizen & CI | `1FXeW4EoofbPkeLOB796ALZBoNsujAwGln-Zaam5OOKA` | `Master Data` | 项目编号列（待确认），N列=进度（目标写入列） |

### 2.2 同步逻辑

1. 读取「项目总表」所有行，解析 E 列 JSON（格式：`[{name, planned, status, owner, ownerEmail}, ...]`）
2. 将每个 milestone 格式化为：`序号|任务描述|计划完成时间|责任人`
   - 序号：milestone 在数组中的位置（从 1 开始）
   - 任务描述：取 JSON `name` 字段 `/` 前的中文部分
   - 计划完成时间：JSON `planned` 字段
   - 责任人：JSON `owner` 字段
3. 多条 milestone 用换行符连接
4. 按 **项目编号**（A列）匹配 Master Data 中对应行，写入 N 列
5. **同步全部项目**（包括 Done 状态），不按状态过滤

### 2.3 输出

- **不发送邮件**，仅写 Log
- Log 写入格式：`writeLog("syncProjectProgress", "成功", "更新 N 行", trigger, "")`

---

## 3. 数据源

| 项目 | 值 |
|---|---|
| 项目跟踪表 ID | `1aoQDjeWU9Xa9clloyTwiXL6WS62tYVbB0-VOavpAgAM` |
| 项目跟踪 Sheet | `项目总表` |
| 项目跟踪匹配列 | A 列（项目编号） |
| 项目跟踪进度列 | E 列（Milestones_JSON） |
| 项目跟踪过滤 | 无（包含所有状态，含 Done） |
| Kaizen & CI 表 ID | `1FXeW4EoofbPkeLOB796ALZBoNsujAwGln-Zaam5OOKA` |
| Kaizen & CI Sheet | `Master Data` |
| Master Data 匹配列 | A 列（项目编号） |
| Master Data 写入列 | N 列（进度） |

---

## 4. 触发方式

| 方式 | 配置位置 | 说明 |
|---|---|---|
| **定时** | `定时设置` sheet → 每天 08:35 | 通过 `02 - 定时执行.js` 的触发器框架调度 |
| **手动** | `菜单设置` sheet → `专项菜单` | 在配置表菜单中手动触发 |

---

## 5. 输出渠道

- **Log**：写入配置表 Log sheet，格式对齐 `writeLog("syncProjectProgress", ...)`
- **无邮件**：同步完成后不发邮件

---

## 6. 配置项

| 配置 | 值 |
|---|---|
| 函数名（定时/菜单） | `syncProjectProgress` |
| 菜单显示名 | `项目进度同步至Kaizen&CI` |
| 定时时间 | 每天 08:35 |
| Log 写入 | `writeLog("syncProjectProgress", ...)` |

---

## 7. 进度格式化规则

E 列 JSON 示例：
```json
[{
  "name": "方案设计/Design",
  "planned": "2026-06-15",
  "status": "进行中",
  "owner": "张三",
  "ownerEmail": "zhangsan@colpal.com"
}]
```

格式化后写入 N 列的文本：
```
1|方案设计|2026-06-15|张三
2|设备采购|2026-07-01|李四
3|安装调试|2026-08-15|王五
```

- **全部同步**，不跳过已完成 milestone
- 跳过 `planned = "NA"` 或为空的 milestone

---

## 8. 边界条件

| 场景 | 处理 |
|---|---|
| 项目总表无数据 | 跳过，Log 记录 |
| E 列 JSON 为空或无法解析 | 跳过该项目，Log 记录解析失败 |
| Master Data 中找不到匹配的项目编号 | 跳过该项目，Log 记录未匹配，N 列保持原值 |
| 项目无有效 milestone 可同步 | N 列保持原值不变 |
| 项目总表 E 列无变化 | 仍全量覆盖写入 N 列（幂等） |

---

## 9. 验收标准

1. 每天 08:35 自动执行，同步进度到 Master Data N 列
2. 专项菜单可手动触发
3. Master Data N 列正确显示格式化后的进度文本
4. 项目编号匹配准确，不串行
5. JSON 解析失败有 Log 记录
6. 未匹配项目有 Log 记录

---

## 10. 已确认事项（V20260606.2）

| # | 事项 | 确认结果 |
|---|---|---|
| 1 | Master Data 中哪一列是项目编号 | A 列 |
| 2 | 是否只同步未完成的 milestone | 全部同步（含已完成） |
| 3 | 匹配不到的项目 N 列如何处理 | 保持原值不变 |
| 4 | 是否只同步特定项目类型 | 同步全部项目（含 Done） |

---

## 11. 技术选型

- 语言：Google Apps Script（JavaScript）
- 文件：`17 - 项目进度同步.js`
- 复用：`01 - Common.js` 的 `writeLog`、`_ms_extractDisplayName`（或直接内联）
- 注册：`02 - 定时执行.js` 定时框架 + `00 - 专项菜单.js` 菜单框架
