# 需求文档 — 项目28：注塑工序日报

## 1. 项目背景
注塑车间管理层需要每天早上查看第二天的工序排班情况，了解各车间各班次的开机数、上班人数和工时安排，以便提前协调资源。当前需要人工查看数据，效率低，需要实现自动化日报推送。

## 2. 核心功能
每日定时获取工时汇总数据，按车间+班次维度聚合，以 HTML 邮件形式发送给指定收件人。

### 2.1 本期确定内容：人员安排信息
按 **车间（TB1/TB2）** × **班次（夜班/早班/中班）** 分组，展示发送日期**第二天**的数据。
TB1 和 TB2 各为独立表格，每表底部含合计行。无数据的班次显示 0：

| 维度 | 指标 | 数据来源 |
|------|------|----------|
| 车间 | TB1 / TB2 | MasterData B列 |
| 班次 | 夜班 / 早班 / 中班 | MasterData A列（日期班次字段） |
| 开机数 | 各班次的开机台数 | MasterData C列 |
| 上班人数 | 各班次安排上班的人数 | MasterData D列（去重计数） |
| 合计工时 | 各班次安排工时总和 | MasterData E列（求和） |

### 2.2 后续可扩展内容
邮件内容结构设计为可扩展，后续可新增其他工序关键信息模块（如实际工时对比、出勤率等）。

## 3. 数据源

| 数据表 | 用途 | 关键列 |
|--------|------|--------|
| `27 - 工时数据汇总.js` → `_pd_TARGET_SHEET_ID` → `MasterData` sheet | 工时排班数据 | A:日期班次, B:车间, C:开机数, D:姓名, E:安排工时 |
| `userID` sheet（权限表） | 收件人过滤 | O列=INJ, P列=S&C, J列=邮箱 |

**日期班次格式**：`YYYY.MM.DD_X班`（如 `2026.06.12_2早`），提取日期部分匹配目标日期。

## 4. 输出渠道

### 4.1 邮件格式
- **标题**：`【注塑工序日报】 YYYY-MM-DD 注塑工序日报`
- **发件人名称**：`工序日报系统`
- **格式**：HTML 富文本邮件，美观友好的表格样式
- **内容结构**：
  - 报表日期说明
  - 按车间分组的数据表格（TB1 / TB2 分开或合并）
  - 包含：班次、开机数、上班人数、合计工时
  - 可扩展区块（预留后续新增内容）

### 4.2 收件人
- **TO**：userID 表中 O列=INJ 且 P列=S&C 的对应邮箱
- **CC**：kelland_zhao@colpal.com（默认）
- **测试阶段**：仅发送至 kelland_zhao@colpal.com

## 5. 触发方式

| 触发类型 | 配置 | 说明 |
|----------|------|------|
| 定时触发 | 每日 08:30 | 配置表「定时设置」新增行 |
| 手动触发 | 菜单项 | 配置表「菜单设置」新增菜单项 |

## 6. 配置项

| 配置项 | 位置 | 说明 |
|--------|------|------|
| 定时设置 | 配置表 `定时设置` sheet | 新增每日 08:30 行，入口函数名待定 |
| 菜单设置 | 配置表 `菜单设置` sheet | 新增菜单项，入口函数名待定 |

## 7. 权限/角色

| 角色 | 说明 |
|------|------|
| 收件人 | userID 表 O=INJ + P=S&C 的用户（注塑车间管理层） |
| CC | kelland_zhao@colpal.com（默认） |
| 发件人 | 脚本运行账号（GAS 部署账号） |

## 8. 边界条件

| 场景 | 处理方式 |
|------|----------|
| 第二天无排班数据 | 邮件正常发送，表格显示"无排班数据" |
| 某班次无数据 | 该班次行开机数/人数/工时显示 0 或 "—" |
| MasterData 无数据 | Log 记录"无数据"，不发送邮件（或发送空报告通知） |
| userID 表无匹配收件人 | Log 警告，仅发送 CC 人 |
| 读取数据异常 | try-catch，writeLog 记录失败，邮件不发 |

## 9. 验收标准

- [ ] 每日 08:30 自动发送邮件至指定收件人
- [ ] 邮件标题格式正确：`【注塑工序日报】 YYYY-MM-DD 注塑工序日报`
- [ ] 邮件内容包含 TB1/TB2 车间、三个班次的开机数、上班人数、合计工时
- [ ] 数据日期为发送日期的第二天
- [ ] HTML 格式美观，表格清晰易读
- [ ] 菜单手动触发功能可用
- [ ] Log 记录正常运行（成功/失败/跳过）
- [ ] 测试邮件仅发送至 kelland_zhao@colpal.com

## 10. 技术选型

| 维度 | 选择 |
|------|------|
| 平台 | Google Apps Script |
| 部署 | clasp push |
| 前端 | 无（纯后端邮件） |
| 依赖 | 复用现有 `writeLog()`、userID 读取逻辑、SpreadsheetApp API |
| 命名规范 | 文件：`28 - 注塑工序日报.js`，入口函数待定，内部函数 `_dr_` 前缀 |
| 数据引用 | 直接 import/引用 `27 - 工时数据汇总.js` 中的 `_pd_TARGET_SHEET_ID` |

## 11. 待确认事项

1. ~~邮件中「第二天」是指发送日期+1？~~ ✅ 已确认
2. ~~三个班次都展示还是仅展示有数据的？~~ ✅ 已确认：全部展示，无数据填 0
3. ~~邮件表格布局：TB1 和 TB2 是分两个独立表格还是一个合并表格？~~ ✅ 已确认：独立表格
4. ~~是否需要总计行？~~ ✅ 已确认：每个表格底部含合计行
5. 入口函数命名建议：`sendDailyProcessReport`

---

> **状态**：✅ 已投产  
> **创建日期**：2026-06-11  
> **确认日期**：2026-06-11  
> **投产日期**：2026-06-11
