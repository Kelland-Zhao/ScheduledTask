# SmartMeeting_Alert 项目记录

## V20260607.2 — 新增保养数据年度备份（20号项目）

**改动内容**：
- 新增文件 `20 - 保养数据备份.js`，入口函数 `backupPMRecords(e)`
- **数据源**：`Database_PM`（`1Y7FclPNn_yHWzwZiRCzSy350fppgXZ3NYgwA1OXQgD4`）→ `PM_Records`
- **日期列**：H列（EndDate），用于判断数据所属年份
- **备份目标**：Drive 文件夹 `1PQyR1bQGVN0a4n1ynGVmq5a3uA2Z2FqD`
- **文件命名**：`EDS_DPM_{年份}_备份时间:{YYYY-MM-DD HH:MM:SS}`
- **逻辑**：与 19 号相同，批量 clear+回写方案
- **内部函数前缀**：`_pm_`
- 配置表 `定时设置` 需新增 1 月每天 08:50 触发行（`backupPMRecords`）
- 配置表 `菜单设置` 需新增菜单项 `保养数据备份`

## V20260607.1 — 新增交接班数据年度备份（19号项目）

**改动内容**：
- 新增文件 `19 - 交接班数据备份.js`，入口函数 `backupShiftRecords(e)`
- **数据源**：`Database_Shift`（`10Fnrqc1AUiPqOi-b2UsKgR-Ww-BNdIla_HB_HjVdI0w`）→ `Shift_Records`
- **日期列**：L列（提交日期），用于判断数据所属年份
- **备份目标**：Drive 文件夹 `1LyqKxmCrTgZZuXGU2fmNQIPk_GaBXgpm`
- **文件命名**：`EDS_DS_{年份}_备份时间:{YYYY-MM-DD HH:MM:SS}`
- **逻辑**：筛选上一年度数据 → 创建备份文件分批写入 → 验证行数 → 从源表删除
- **触发**：仅 1 月每天 08:45；无上年数据时 Log 记录"跳过"
- **内部函数前缀**：`_sb_`
- 配置表 `定时设置` 需新增 1 月每天 08:45 触发行（`backupShiftRecords`）
- 配置表 `菜单设置` 需新增菜单项 `交接班数据备份`

## V20260606.5 — 新增质量巡场未解决问题每日提醒（18号项目）

**改动内容**：
- 新增文件 `18 - 质量巡场问题提醒.js`，入口函数 `sendQualityTourIssues(e)`
- **数据源**：2026注塑月度质量巡场记录（`1O-cwRTfW7jB-TSAeDDNvi_LyEM-Dhsi_Y2AqIwbku4c`）
- **扫描范围**：所有含 `2026` 的 sheet，排除「巡场模板」（当前 6 个：TB1/TB2 2026.03~05）
- **未解决判定**：I列（当前状态）≠ `Done`（大小写不敏感，trim 后匹配），Ongoing/转SDM/空值均算未解决
- **邮件**：一封合并邮件，按 Sheet 分组展示，标题 `[质量巡场] 未解决问题汇总 YYYY-MM-DD`
- **收件人**：userID 表 O=INJ + P=S&C（同 15 号项目）
- 配置表 `定时设置` 新增每日 08:40 触发行（`sendQualityTourIssues`）
- 配置表 `菜单设置` 新增菜单项 `质量巡场问题提醒`
- 发件人名称：`Quality Tour Alert`

## V20260606.4 — 新增项目进度同步至 Kaizen & CI（17号项目）

**改动内容**：
- 新增文件 `17 - 项目进度同步.js`，包含函数 `syncProjectProgress(e)`
- **数据源**：项目跟踪表（`1aoQDjeWU9Xa9clloyTwiXL6WS62tYVbB0-VOavpAgAM`）→「项目总表」
- **写入目标**：Kaizen & CI 表（`1FXeW4EoofbPkeLOB796ALZBoNsujAwGln-Zaam5OOKA`）→「Master Data」N列
- **匹配键**：A列项目编号
- **同步格式**：E列 JSON → `序号|任务描述|计划完成时间|责任人`，多条换行分隔
- **同步范围**：全部项目（含 Done），全部 milestone（含已完成），跳过 planned=NA 或为空
- **覆盖策略**：仅匹配成功且有进度内容时写入 N列；未匹配或进度为空则保持原值
- 配置表 `定时设置` 需新增每日 08:35 触发行（`syncProjectProgress`）
- 配置表 `菜单设置` 需新增菜单项 `项目进度同步至Kaizen&CI`
- 内部函数用 `_sp_` 前缀

## V20260606.3 — Milestone邮件提醒重构（自定义milestone + 责任人 + 上级CC）

**改动内容**：
- 移除 `MS_KEYWORDS` 关键词匹配 → 直接取 JSON `name` 字段 `/` 前中文作为显示名
- 触发条件：`actual` 为空 → `status !== "已完成"`（适配 JSON 新增的 status 字段）
- 新增 `_ms_buildUserLookup()`：读取 userID 表构建 Leader 上级映射（BI列）和 INJ工序管理员列表（O列=INJ + BH列有标记 → J列邮箱）
- 新增 `_ms_extractDisplayName()`：从完整名称提取中文显示名
- TO：Leader（B列提取）+事项责任人（JSON `ownerEmail`）
- CC：Leader上级 + INJ工序管理员 + kelland_zhao@colpal.com（不再强制 Lyon）
- 标题简化：`【项目逾期】有 N 个Milestone已逾期` / `【项目临期】有 N 个Milestone即将到期`
- JSON 解析失败增加 `writeLog` 写 Log 表（原仅 console.warn）
- 所有 `const/let` → `var`（兼容性）

## V20260605.01 — 点检机台核对功能新增

**改动内容**：
- 新增文件 `15 - 点检机台核对.js`，包含函数 `checkPointCheckMachines(e)`
- 功能：比对 MachineList(工序=INJ) 与 Workcenter(C列≠闲置) 的机台差异，差异1（点检有/计划账无）MachineList 标黄+邮件通知，差异2（计划账有/点检无）仅邮件通知
- 点检表：`MachineList`（ID: `1RQql-PrcBWiAQNeg7hQKcocpllSUMRhT5XPrDTVWoBY`）
- 计划账表：`Workcenter`（ID: `12MXO53wJC8s_J-IE2uGY5jx35rnUE7rxW1xvwVU-FxM`）
- 权限表：`userID`（ID: `1F7G3WOY5xM4fEYZ1s5RKulY4kJhqCZ9HefthmiVkraM`），过滤 O列=INJ + P列=S&C
- 配置表 `定时设置` 新增每日 08:25 触发行（`checkPointCheckMachines`）
- 配置表 `菜单设置` 新增菜单项 `点检机台核对`

## V20260604.01 — Workcenter 数据同步功能新增

**改动内容**：
- 新增文件 `14 - Workcenter数据同步.js`，包含函数 `syncWorkcenterData(e)`
- 功能：从 Line Database 读取生产拉线数据，过滤有效 Individual Machine，智能转换 Final Machine Type，通过 Equipment_Number_EAM 的 K列匹配设备编号，判断主设备标识，全量写入 Workcenter 的 A-F 列（共 6 个字段）
- 源数据表：`1. Line Database`（ID: `11zyH65MhC-LuqsEXT6KeO3-GQ3jwW7z7kJjHD0TwLZc`）
- 字典表：`Equipment_Number_EAM`（只读，K列→A列映射）
- 目标表：`Workcenter`（ID: `12MXO53wJC8s_J-IE2uGY5jx35rnUE7rxW1xvwVU-FxM`）
- 配置表 `定时设置` 需新增每日 08:20 触发行（`syncWorkcenterData`）
- 配置表 `菜单设置` 需新增菜单项 `Workcenter数据同步`
- **修正**：原项目匹配列从 M 列改为 K 列（符合业务逻辑）

## V20260525.01 — IoT数据备份功能新增

**改动内容**：
- 新增文件 `4 - IoT数据备份.js`，包含函数 `copyIoTDetailToFolder()`
- 功能：将 `IoT_3AX_6AX_FullOperaFCS` 中 `IoT_CT_Detail` sheet 的数据复制到指定 Drive 文件夹，每日生成一个 Spreadsheet（命名格式：`IoT_CT_Detail_YYYY-MM-DD`），当日多次运行幂等
- 源数据不清空
- 配置表（`1VH9I4-vRA3GlKo2dx6LVsr29HKdzCylxwROdsfoLwtM`）`定时设置` 新增每日 8:00 触发行（`copyIoTDetailToFolder`）
- 配置表 `菜单设置` 新增菜单项 `IoT数据备份-手动`

## V20260526.01 — 新增企微推送、Gmail月报、数据诊断脚本

**改动内容**：
- 新增 `3 - 企微推送.js`：早晚企微群推送（`sendWechatMorning` / `sendWechatEvening`），按授权人分组展示未完成跟进项及本月完成率
- 新增 `5 - Gmail月报.js`：每周一合并发送月报（`sendGmailReports`），含团队总览、个人完成率排行、当月未完成清单
- 新增 `6 - 数据诊断.js`：手动工具，`previewData()` 数据预览、`diagnoseAssignees()` 跟进人白名单对账诊断

## V20260526.02 — 新增 PM KPI 数据写入功能

**改动内容**：
- 新增 `7 - PM_KPI写入.js`，包含函数 `writePMKPI()`
- **数据源**：`202509 - Post-PM Performance Analysis Data`（`1C7z16Ww22dfGxeqKRrBw27J-zmXXStAt2xoEYwL-V24`）中 `Master Data` sheet
- **排除条件**：读取同源表 `KPI排除条件` sheet，排除状态=启用的 Workcenter
- **写入目标**：`E&E Technician Bonus`（`1KYRwzlYZz9OK8NPAkndhc9MxGlLEILYsRbsMkSvcV_U`）中 `3.PM_KPI` sheet
- **Process 映射**：`AFT & USW`→AFT，`HSL & Bou`→HSL+BOU，`Zar`→ZAH，TF=子工序汇总，IM/PK 直接对应；SubProcess="TF" 时通过 `TF Machine Type` 表补充分类
- **写入方式**：A/B 列（Month、Process）已预填，读取 A:D 找到 C 为空的行，按 A/B 匹配后将计算结果写入 C/D（Total PM Count、UPDT Reduction Valid Count）；E:J 均为 ArrayFormula 自动计算
- **计算逻辑**：Total=过滤后行数，UPDT Valid Count=绩效状态"好/Good"行数
- **幂等**：仅写入 C 列为空的行，已有数据的行跳过

## V20260601.03 — 新增注塑工艺周检模块

**改动内容**：
- 新增 `9 - 注塑工艺周检.js`，将 `08 - Weekly_Process_Inspection` 项目的核心功能迁移至本项目
- **定时触发入口**：`runWeeklyInspection()`
- **功能**：从机台数据库、检查记录表拉取数据，匹配后写入 Master Data；含上周漏匹配补录逻辑
- **排除**：日报/周报邮件功能（dailyReport/weeklyReport）不迁移
- **函数命名规范**：所有内部函数加 `_wi` 前缀，避免与项目内其他函数冲突
- **数据源**：机台库 `1BeoCokGiWAdkfFTSVOkxNr4Gr9O6FlnwTvnOmLYNY_U`，检查记录 `18hoqx_pnoRrjqijiDCOWz_hnyu3EUSWS-_sWKAwh868`，Master Data `1afvNifotG_Ik36NQ7aptPjKT4ebAyeSBCc4hJ9WL7v4`

---

## V20260530.01 — 新增 Kaizen KPI 数据写入功能

**改动内容**：
- 新增 `8 - Kaizen_KPI写入.js`，包含函数 `writeKaizenKPI()`
- **数据源1**：`Kaizen_Year`（`1R0O_GfCQyQWXJ6ZR5AfE1xgCOuPrMP3FFOZ6u3I9rLg`）
- **数据源2**：`保养组人员名单`（目标表同表）
- **写入目标**：`Master_Data`（`1eeG8wA9YbCZfNv6qndDKa3vs7VKs2MT2fFG12Tk-At4`）
- **过滤条件**：`部门 = EQU` + `项目负责人工号` 在保养组名单中
- **统计逻辑**：按 月份+工序 分组；C列=总提交数，D列=优秀备注="优秀"的行数，E列（季度优秀）暂不填
- **写入方式**：清空 Master_Data 数据区（保留表头）后重新写入，结果按月份+工序升序排列
- 配置表 `定时设置` 新增每日 08:00 触发行（`writeKaizenKPI`）
- 配置表 `菜单设置` 新增菜单项 `Kaizen数据写入`

## V20260603.01 — 新增辅导与反馈提醒模块

**改动内容**：
- 新增 `10 - 辅导与反馈提醒.js`，包含函数 `sendCoachingReminder()`
- **数据源**：`计时员工辅导与反馈表名单`（`1XyYXB6RCkgZGaAuVv73b1gHOx81w1oOb50QtqTX_Xyk`）Sheet：`1-12 月辅导与反馈`，数据从第 6 行开始
- **未缴判定**：G列（已经提交）为空则为未缴；`Y`=已提交，其他非空（退休/考勤工伤等）=豁免，均跳过
- **提醒逻辑**：按 H列 工序（TF/PK/IM）分组，每工序发一封邮件；若所有记录均已缴则不发邮件
- **TO**：F列辅导工程师（通过 userID 表姓名匹配 GMail 列获取邮箱，含姓名预处理去掉 `-Z`/`_QC` 类后缀）
- **CC**：各工程师直线领导（userID BI列 `直线上级邮箱`）+ `judy_zhang@colpal.com` + `kelland_zhao@colpal.com`
- **Log**：每次运行均写入 Log（跳过/成功/失败/警告均记录）
- 配置表 `菜单设置` 新增菜单项 `辅导与反馈提醒-手动`（`sendCoachingReminder`）
- 配置表 `定时设置` 新增每日 8:15 触发行（`sendCoachingReminder`）
- 内部函数统一使用 `_cf_` 前缀，避免命名冲突

## V20260605.05 — 点检机台核对豁免规则扩展

**改动内容**：
- `15 - 点检机台核对.js`：差异类型1新增机型 `Plasma` 豁免（MachineList C列）
- `15 - 点检机台核对.js`：差异类型2新增指定 Workcenter ID 豁免：`V2FTA164`、`V2FTA264`、`V2FTA364`

## V20260605.04 — 点检机台核对差异类型2新增 DP/HS 豁免

**改动内容**：
- `15 - 点检机台核对.js`：差异类型2豁免条件从 `6AX` 扩展为 `["6AX", "DP", "HS"]`

## V20260605.03 — 点检机台核对邮件机型改为 Final Machine Type

**改动内容**：
- `15 - 点检机台核对.js`：`machineModel` 取值从 C列（机器性能，index 2）改为 D列（Final Machine Type，index 3）
- 邮件差异类型2表头「机型」改为「Final Machine Type」

## V20260605.02 — 点检机台核对邮件机型字段修正

**改动内容**：
- `15 - 点检机台核对.js`：差异类型2邮件中【机型】列取值从 Workcenter G列改为 C列（机器性能）

## V20260605.01 — 同步 GAS 最新版本至 GitHub

**改动内容**：
- 所有文件从单位数前缀（`0-`、`1-`...`9-`）重命名为双位数前缀（`00-`、`01-`...`13-`）
- 新增模块 `08 - 故障报告通知.js`
- 新增模块 `11 - 保养反馈同步.js`
- 新增模块 `14 - Workcenter数据同步.js`
- 新增模块 `15 - 点检机台核对.js`
- `10 - 辅导与反馈提醒.js` 同步 GAS 最新内容（轻微格式调整）

## V20260601.01 — 注塑周检添加 Log 写入及手动/定时触发区分

**改动内容**：
- `9 - 注塑工艺周检.js`：`_weeklyInspectionMain()` 新增 `trigger` 参数，执行成功/失败均调用 `writeLog()` 写入 Log
- 新增手动触发入口 `runWeeklyInspectionManual()`，传入 `"手动"`；原 `runWeeklyInspection()` 传入 `"定时"`
- 配置表 `菜单设置` B16 由 `runWeeklyInspection` 改为 `runWeeklyInspectionManual`，确保菜单点击记录触发方式为"手动"

## V20260606.01 — Milestone邮件提醒功能迁移（16号）

**改动内容**：
- 从 Project_Tracking 迁移 Milestone 邮件提醒至 ScheduledScripts
- 新增文件 `16 - Milestone邮件提醒.js`，入口函数 `milestoneReminder()`
- **数据源**：`PROJECT_TRACKING_SPREADSHEET_ID` = `1C7z16Ww22dfGxeqKRrBw27J-zmXXStAt2xoEYwL-V24`，读取「项目总表」
- **业务逻辑**：扫描 Ongoing 项目各 Milestone 计划时间（提前2天/提前1天/到期当天/超期），按 Leader 合并为一封邮件发送
- **邮件模板**：中英双语，逾期红色/临期橙色区分（渐变色表头+圆角徽章）
- **收件人**：TO=Project Leader + lyon_zhang@colpal.com，CC=kelland_zhao@colpal.com，发件人名称「项目Milestone提醒系统」
- **Log**：写入 ScheduledScripts 配置表 Log sheet（列 A=E: 日期/项目名/Milestone/去重Key/触发类型 + 汇总行）
- 配置表 `菜单设置` 新增「Milestone邮件提醒」（`milestoneReminder`，启用）
- 配置表 `定时设置` 新增每日 12:30 触发（`milestoneReminder`）
