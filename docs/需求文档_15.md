# 需求文档 — 15 号项目：注塑点检机台核对

> 版本：V20260604.1 | 状态：待确认

---

## 1. 项目背景

注塑车间有两套机台数据：
- **点检系统**：实际执行点检的机台清单（MachineList）
- **计划账**：排产计划中的机台清单（Workcenter）

两边数据可能不一致，需要每日自动核对差异，确保点检覆盖所有计划账机台、计划账不遗漏点检机台。

---

## 2. 核心功能

### 2.1 数据读取
| 数据源 | 表 | Sheet | 过滤条件 | 比对键 |
|---|---|---|---|---|
| 点检侧 | `1RQql-...` Database_PointCheck | MachineList | 工序(A列) = `INJ` | 机台号(D列) |
| 计划账侧 | `12MXO53...` 注塑计划机台 | Workcenter | C列(机器性能) ≠ `闲置` | Workcenter(A列) |

### 2.2 差异比对
| 差异类型 | 定义 | MachineList 标记 | 邮件说明 |
|---|---|---|---|
| **类型 1** | 点检有 / 计划账无 | 整行标黄 | 「请确认机台是否存在，更新点检机台主数据」 |
| **类型 2** | 计划账有 / 点检无 | 不做标记 | 「这些机台在计划账上，但在点检机台主数据中缺失，需要更新点检机台主数据」 |
| **豁免** | Workcenter G列(机型) = `6AX` 的机台，不纳入差异类型2 | — | — |

### 2.3 输出

**Gmail 邮件：**
- 收件人：权限表 `userID` 中 `O列(工序)=INJ` 且 `P列(职位)=S&C` 的人（取 J列 GMail）
- **一封邮件**，内含两个区块：
  - 差异类型 1：点检有/计划账无 的机台清单
  - 差异类型 2：计划账有/点检无 的机台清单

**MachineList 标记：**
- 差异类型 1 的机台：在 MachineList 中匹配到的行整行标**黄色背景**
- 差异类型 2：**不在** MachineList 中标色（仅邮件通知）

---

## 3. 数据源

| 项目 | 值 |
|---|---|
| 点检表 ID | `1RQql-PrcBWiAQNeg7hQKcocpllSUMRhT5XPrDTVWoBY` |
| 点检 Sheet | `MachineList` |
| 计划账表 ID | `12MXO53wJC8s_J-IE2uGY5jx35rnUE7rxW1xvwVU-FxM` |
| 计划账 Sheet | `Workcenter` |
| 权限表 ID | `1F7G3WOY5xM4fEYZ1s5RKulY4kJhqCZ9HefthmiVkraM` |
| 权限 Sheet | `userID` |
| 权限过滤条件 | O列(工序) = `INJ`，P列(职位) = `S&C` |
| 邮件列 | J列（GMail） |

---

## 4. 触发方式

| 方式 | 配置位置 | 说明 |
|---|---|---|
| **定时** | `定时设置` sheet → 每天 08:25 | 通过 `02 - 定时执行.js` 的触发器框架调度 |
| **手动** | `菜单设置` sheet → `专项菜单` | 在配置表菜单中手动触发 |

---

## 5. 输出渠道

- **Gmail**：HTML 表格邮件，标题：`[点检核对] 注塑机台差异报告 YYYY-MM-DD`
- **MachineList 标记**：差异类型 1 机台整行黄色背景

---

## 6. 配置项

| 配置 | 值 |
|---|---|
| 函数名（定时/菜单） | `checkPointCheckMachines` |
| 菜单显示名 | `点检机台核对` |
| 定时时间 | 每天 08:25 |
| Log 写入 | 同其他项目，`writeLog("checkPointCheckMachines", ...)` |
| 发件人名称 | `PointCheck Alert` |

---

## 7. 权限/角色

无需额外权限控制。收件人由 `userID` 表动态过滤（O=INJ + P=S&C）。

---

## 8. 边界条件

| 场景 | 处理 |
|---|---|
| Workcenter C列含「闲置」的机台 | 排除，不参与比对 |
| 两边数据完全一致 | 邮件报告「无差异」，MachineList 不做标记 |
| MachineList 无 INJ 数据 | 跳过，Log 记录 |
| Workcenter 有效数据为空 | 跳过，Log 记录 |
| 权限表无匹配收件人 | 跳过邮件发送，Log 记录 |
| 之前标黄的旧差异已消除 | 清除 MachineList 原有黄色标记后再重新标记 |

---

## 9. 验收标准

1. 每天 08:25 自动执行，发送差异报告邮件
2. 专项菜单可手动触发
3. 邮件正确列出两类差异机台
4. MachineList 中差异类型 1 机台整行标黄
5. 差异消除后黄色标记自动清除
6. Log 中正确记录每次执行状态

---

## 10. 技术选型

- 语言：Google Apps Script（JavaScript）
- 文件：`15 - 点检机台核对.js`
- 复用：`01 - Common.js` 的 `writeLog`、`sendMail`、`buildHtmlTable` 等公共函数
- 注册：`02 - 定时执行.js` 定时框架 + `00 - 专项菜单.js` 菜单框架
